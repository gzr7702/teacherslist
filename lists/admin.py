from django.contrib import admin
from lists.models import Teacher, Parent, Subject, Student

admin.site.register(Teacher)
admin.site.register(Parent)
admin.site.register(Subject)
admin.site.register(Student)
